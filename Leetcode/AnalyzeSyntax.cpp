/**
 * Author: Imran Parthib
 * inspired from Tourist the legend
 */

#include <bits/stdc++.h> // Includes all standard C++ libraries (non-standard but common in contests)
using namespace std;     // Avoids writing std:: everywhere

// Debugging macro:
// If compiled with -DLOCAL, includes a custom debug header for local testing.
// Otherwise, defines debug(...) as a no-op (does nothing) to avoid errors on online judges.
#ifdef LOCAL
#include "algo/debug.h"
#else
#define debug(...) 42
#endif

int main()
{
    // main function entry point
    // Contest code will go here
    return 0; // Successful program termination
}
